$!/bin/bash

# Inject some additional JSON key/value pairs
json -I -f /tmp/instruqt.json \
    -e 'this.Env.INSTRUCT_CHALLENGE_NAME="Verify Your Bolt Installation"' \
    -e 'this.Env.INSTRUCT_CHALLENGE_STATE="start"'